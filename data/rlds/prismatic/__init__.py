# Minimal vendored slice of OpenVLA's `prismatic` package: only the OXE/RLDS data
# pipeline needed for LIBERO streaming. The upstream top-level __init__ imported the
# full model stack (vision/LLM backbones, timm, etc.); that is intentionally omitted.
